[package]
name = "verbatim-config"
description = "Configuration: global settings.toml plus layered profiles from a profiles folder, both next to the executable"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
serde.workspace = true
toml.workspace = true

[lints]
workspace = true

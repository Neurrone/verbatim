[package]
name = "verbatim-synth-capture"
description = "Capture synth for tests: records utterances and timestamps instead of producing audio"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
verbatim-audio.workspace = true
verbatim-model.workspace = true
verbatim-speech.workspace = true

[lints]
workspace = true
